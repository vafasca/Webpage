[build]
  command = "npm install && ng build --configuration=production"
  publish = "dist/webpage"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.processing.css]
  bundle = false
  minify = false
